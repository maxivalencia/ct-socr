{% extends "ct/ct.html.twig" %}

{% block title %}Contrôle Technique - Profession - Edition{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="row">
            <h1>Edit Professions</h1>
        </div>

        {{ include('professions/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('professions_index') }}">back to list</a>

        {{ include('professions/_delete_form.html.twig') }}
    </div>
{% endblock %}
