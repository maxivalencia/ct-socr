{% extends "ct/ct.html.twig" %}

{% block title %}Contrôle Technique - Profession - Details{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="row">
            <h1>Professions</h1>
        </div>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ profession.id }}</td>
                </tr>
                <tr>
                    <th>Profession</th>
                    <td>{{ profession.profession }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('professions_index') }}">back to list</a>

        <a href="{{ path('professions_edit', {'id': profession.id}) }}">edit</a>

        {{ include('professions/_delete_form.html.twig') }}
    </div>
{% endblock %}
