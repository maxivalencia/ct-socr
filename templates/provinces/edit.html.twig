{% extends "ct/ct.html.twig" %}

{% block title %}Provinces - Edition{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="row justify-content-center align-items-center kl-sous-titre">
            <h1>Edition Provinces</h1>
        </div>
        <br>
        {{ include('provinces/_form.html.twig', {'button_label': 'Modifier'}) }}
        <br>
        <a href="{{ path('provinces_index') }}" class="btn btn-primary kl-bouton"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour vers la liste</a>
        <br><br>
        {{ include('provinces/_delete_form.html.twig') }}
    </div>
{% endblock %}
