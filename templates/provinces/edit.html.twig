{% extends "ct/ct.html.twig" %}

{% block title %}Contrôle Technique - Province - Edition{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="row">
            <h1>Edition Provinces</h1>
        </div>

        {{ include('provinces/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('provinces_index') }}">back to list</a>

        {{ include('provinces/_delete_form.html.twig') }}
    </div>
{% endblock %}
