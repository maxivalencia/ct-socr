{% extends "ct/ct.html.twig" %}

{% block title %}Contrôle Technique - Province - Details{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="row  justify-content-center align-items-center kl-sous-titre">
            <h1>Provinces</h1>
        </div>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ province.id }}</td>
                </tr>
                <tr>
                    <th>Province</th>
                    <td>{{ province.province }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('provinces_index') }}" class="btn btn-primary">Retour vers la liste</a>
        <br><br>
        <a href="{{ path('provinces_edit', {'id': province.id}) }}" class="btn btn-success">edit</a>
        <br><br>
        {{ include('provinces/_delete_form.html.twig') }}
    </div>
{% endblock %}
