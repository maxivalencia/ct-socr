{% extends "ct/ct.html.twig" %}

{% block title %}Contrôle Technique - Role - Details{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="row justify-content-center align-items-center kl-sous-titre">
            <h1>Roles</h1>
        </div>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ role.id }}</td>
                </tr>
                <tr>
                    <th>Role</th>
                    <td>{{ role.role }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('roles_index') }}" class="btn btn-primary">Retour vers la liste</a>

        <a href="{{ path('roles_edit', {'id': role.id}) }}" class="btn btn-success">edit</a>

        {{ include('roles/_delete_form.html.twig') }}
    </div>
{% endblock %}
