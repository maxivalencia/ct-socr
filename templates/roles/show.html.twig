{% extends "ct/ct.html.twig" %}

{% block title %}Contrôle Technique - Role - Details{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="row justify-content-center align-items-center kl-sous-titre">
            <h1>Roles</h1>
        </div>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ role.id }}</td>
                </tr>
                <tr>
                    <th>Role</th>
                    <td>{{ role.role }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('roles_index') }}" class="btn btn-primary"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour vers la liste</a>
        <br><br>
        <a href="{{ path('roles_edit', {'id': role.id}) }}" class="btn btn-success"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Modifier</a>
        <br><br>
        {{ include('roles/_delete_form.html.twig') }}
    </div>
{% endblock %}
