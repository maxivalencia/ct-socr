{% extends "ct/ct.html.twig" %}

{% block title %}Rôle - Edition{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="row justify-content-center align-items-center kl-sous-titre">
            <h1>Edition Roles</h1>
        </div>
        <br>
        {{ include('roles/_form.html.twig', {'button_label': 'Modifier'}) }}
        <br>
        <a href="{{ path('roles_index') }}" class="btn btn-primary kl-bouton"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour ver la liste</a>
        <br><br>
        {{ include('roles/_delete_form.html.twig') }}
    </div>
{% endblock %}
