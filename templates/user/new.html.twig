{% extends "ct/ct.html.twig" %}

{% block title %}Utilisateurs - Nouveau{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="row justify-content-center align-items-center kl-sous-titre">
            <h1>Ajouter un nouveau utilisateur</h1>
        </div>

        {{ include('user/_form.html.twig') }}
        <br>
        <a href="{{ path('user_index') }}" class="btn btn-primary kl-bouton"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour vers la liste</a>
    </div>
{% endblock %}
