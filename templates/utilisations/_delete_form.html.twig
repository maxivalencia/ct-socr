<form method="post" action="{{ path('utilisations_delete', {'id': utilisation.id}) }}" onsubmit="return confirm('Etes-vous sûre de vouloir supprimer cet utilisation?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ utilisation.id) }}">
    <button class="btn btn-danger kl-bouton"><i class="fa fa-trash" aria-hidden="true"></i> Supprimer</button>
</form>
