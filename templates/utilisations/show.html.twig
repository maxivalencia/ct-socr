{% extends "ct/ct.html.twig" %}

{% block title %}Utilisations - Details{% endblock %}

{% block contenue %}
    <div class="container">
        <div class="row justify-content-center align-items-center kl-sous-titre">
            <h1>Utilisations</h1>
        </div>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ utilisation.id }}</td>
                </tr>
                <tr>
                    <th>Utilisation</th>
                    <td>{{ utilisation.utilisation }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('utilisations_index') }}" class="btn btn-primary kl-bouton"><i class="fa fa-arrow-left" aria-hidden="true"></i> Retour vers la liste</a>
        <br><br>
        <a href="{{ path('utilisations_edit', {'id': utilisation.id}) }}" class="btn btn-success kl-bouton"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Modifier</a>
        <br><br>
        {{ include('utilisations/_delete_form.html.twig') }}
    </div>
{% endblock %}
