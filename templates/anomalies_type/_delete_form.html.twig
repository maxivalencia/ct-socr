<form method="post" action="{{ path('anomalies_type_delete', {'id': anomalies_type.id}) }}" onsubmit="return confirm('Etes-vous sûre de vouloir supprimer ce type d\'anomalie?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ anomalies_type.id) }}">
    <button class="btn btn-danger kl-bouton"><i class="fa fa-trash" aria-hidden="true"></i> Supprimer</button>
</form>
